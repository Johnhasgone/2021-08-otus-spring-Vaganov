spring:
  datasource:
    url: jdbc:h2:mem:bookdb
  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true

  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true
  liquibase:
    enabled: true
    drop-first: true